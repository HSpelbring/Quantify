To Add/Fix:
- Main App Logo
- Graph (Add Dashed Line to show starting day value)
- Store Values to Improve Graph (day graph)
- Animated background
- Toggleable Portfolio Overview
Dashboard
Dynamic Navbar to get to other pages


Lookup 
Search Bar to search stocks
Favorites tab - favorite stocks
ability to favorite stocks
top gainers? - way later
Current price
daily change
intraday history with toggles for 1d/5d/1m/3m/1Y/ytd/5Y
Market cap, pe, volumes, small company profile summary, etc
user search history
snapshots of historical price

Portfolio
Ability to buy / sell stocks (i.e. fake sell/buy instead of add and remove stocks)
Essentially a simulation of a real Portfolio
Overall graph with toggles for 1d/5d/1m/3m/etc
Overall price gain/percent gain 

PORTFOLIO OVERVIEW (Top-Right Box)

The box you already have — we expand it cleanly.

Data shown:

Total portfolio value

Daily % gain/loss

Daily dollar value change

Sparkline chart of overall portfolio

Best + worst performers (optional upgrade)

Backend Calls

GET /portfolio/value
Returns { value, daily_change, history }

Later, you will feed this with simulated positions in the Portfolio page.
For now, this is a placeholder using:

portfolio.price = sum(funds.price)
portfolio.change = sum(funds.change)
portfolio.history = combinedIndexHistory

Caching Strategy

Portfolio value: recompute every 1–5 seconds

Portfolio history: cached for 30–60 seconds

Holdings fetched from DB: cached for 5 minutes

2️⃣ INTERACTIVE PORTFOLIO GRAPH

This sits inside the Portfolio box.

Graph features:

Line graph

Toggle: 1D, 5D, 1M, 3M, 1Y, YTD, 5Y, MAX

Hover values

Automatic color transition (green ↔ red)

Smooth animation

Shaded under-curve

Data Source

For now: combine indexes or selection of user holdings.

Later: compute from historical prices + user’s transactions.

Backend Route
GET /portfolio/history?range=1d|5d|1m|...

Caching

Hard cache for 1–5m

Daily snapshots stored in DB

3️⃣ MARKET OVERVIEW SECTION (Optional, but HIGH VALUE)

This is the middle or left section of the dashboard.

Indexes to show:

S&P 500 → ^GSPC

NASDAQ 100 → ^NDX

Dow Jones → ^DJI

Russell 2000 → ^RUT

VIX → ^VIX

Crypto (BTC-USD, ETH-USD)

Each card shows:

Symbol

Name (S&P 500)

Current price

Daily % change

Mini sparkline (10–50 points)

Color-coded (green/red)

Backend Endpoints

GET /funds (your existing endpoint)

But reorganize response to:

{
  "^GSPC": { price, change, history },  
  "^NDX": { ... },
  ...
}


You already fixed history for these — next step is improving reliability.

Caching Layer

Use a tiered approach:

Data Type	TTL
Intraday prices	5–10 sec
Daily summary (open/high/low/close)	60s
Fundamentals	6–12 hours
4️⃣ SCROLLING TICKER BAR (Bottom)

You already built this, and it looks awesome visually.

Now we refine it.

Ticker Items should show:

Symbol

Name

Daily % change

Current price

Sparkline mini-chart

Fix the issue you had (1,2,3 labels)

We update your Angular to map:

fund.symbol

fund.name

Instead of using index keys.

Animation

You already implemented a smooth infinite scroll.
Ideal duration: 45–60 seconds for 50 tickers.

Backend Source

Top 20–50 tracked tickers:

Major indexes

Major ETFs

Popular stocks (AAPL, MSFT, TSLA, NVDA…)

Major crypto assets

Stored in:

tracked.go


Where each item has:

Symbol
Name
Category (Index, ETF, Stock, Crypto)

🌩 BACKEND ARCHITECTURE — FOR DASHBOARD ONLY
Go backend (real-time price proxy + caching)

Endpoints powering the entire dashboard:

GET /api/funds         // All tracked tickers with prices + history
GET /api/fund/:symbol  // Individual ticker
GET /api/portfolio/value
GET /api/portfolio/history   // later